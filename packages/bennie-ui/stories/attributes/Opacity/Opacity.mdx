import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { Text } from '@bennie-ui/text'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/opacity'

opacity
## Example
```
<Section opacity='0'>Content</Section>
```
## Types
[See Opacity values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/sizing/opacity.types.ts)


# Compiled tests
{
  variants.base.map((value, idx) => {
    const opacity_value =  value.split('-')[1]

    return <Unstyled key={idx}>
     <Section opacity={opacity_value} className="border bg-gray-200 border-gray-200">
        opacity: {opacity_value}
     </Section> 
    </Unstyled>
  })
}


