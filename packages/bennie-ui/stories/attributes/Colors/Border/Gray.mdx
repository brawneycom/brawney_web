import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { Text } from '@bennie-ui/text'
import { variants } from '@bennie-ui/baseline/tailwind/colors/border/sm'

# Demo of supported border values

## Example
```
<Section colors={{
    border: { color: 'gray', weight: '400' }
  }}>
  Content
</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/sizing/width.types.ts)


# Compiled tests
{
  variants.base.gray.map((value, idx) => {
    const border_values = value.split('-')
    const border_color = border_values[1]
    const border_weight = border_values[2]

    return <Unstyled key={idx}>
     <Section 
        border={{ width: { all: '1' } }}
        colors={{ border: { color: border_color, weight: border_weight } }}>
        {value}
     </Section> 
    </Unstyled>
  })
}


