import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { Text } from '@bennie-ui/text'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/align'

# Demo of supported text "align" values

## Example
```
<Section align={{ value: 'left' }}>Content</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/sizing/width.types.ts)


### Compiled tests
{
  variants.base.map((value, idx) => {
    const align_value =  value.split('-')[1]

    return <Unstyled key={idx}>
     <Section className="border" width={{value: 'w-full'}} align={align_value}>
        {align_value}
     </Section> 
    </Unstyled>
  })
}


