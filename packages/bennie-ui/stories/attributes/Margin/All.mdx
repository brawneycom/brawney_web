import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/margin'

# Demo of supported padding values
Tailwind supports values up to padding(96), initially bennie-ui will go as far as 40.

## Example
```
<Section margin={{ all: '0' }}Content</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/spacing/padding.types.ts)

## Compiled Tests

{
  variants.base.all.map((value, idx) => {
    const [, m_value] = value.split('-')
    return <Unstyled key={idx}>
      <Section width={{ value: 'full'}} className="border">
        <Section key={idx} 
          margin={{ all: m_value }} 
          width={{ value: 'fit'}}
          height={{ value: 'fit'}}
          className="border border-sky-500">
            {value}
        </Section>
      </Section>
    </Unstyled>
  })
}


