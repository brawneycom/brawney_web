import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/margin'

# Demo of supported padding values
Tailwind supports values up to margin(96), initially bennie-ui will go as far as 40.

## Example
```
<Section margin={{ y: '0' }}Content</Section>
```
## Types
[See margin Values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/spacing/margin.types.ts)

## Compiled Tests
{
  variants.base.y.map((value, idx) => {
    const [, m_value] = value.split('-')
    return <Unstyled key={idx}>
      <Section width={{value: 'full'}} className="border">
        <Section key={idx} 
          margin={{ y: m_value }} 
          width={{ value: 'fit'}}
          height={{ value: 'fit'}}
          className="border border-sky-500">
            {value}
        </Section>
      </Section>
    </Unstyled>
  })
}


