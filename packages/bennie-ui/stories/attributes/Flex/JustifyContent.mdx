import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { variants } from '@bennie-ui/baseline/tailwind/display/flex'

# Flex 

## JustifyContent

Example
```
<Section flex={{ justifyContent : "normal" }}>
    <Section>Item 1</Section>
    <Section>Item 2</Section>
    <Section>Item 3</Section>
  </Section>

```

# Compiled tests (row)
{
  variants.base.justifyContent.map((value, idx) => {
    const jc_value = value.split('-')[1]

    return <Unstyled>
      <Section margin={{y: '1'}} flex={{ justifyContent: jc_value}} width={{value: 'full'}} colors={{ background: { color: 'teal'}}}>
        <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
        <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
        <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
      </Section>
    </Unstyled>
  })
}

## Notes

stretch looks more like is combined with grid-flow-col https://tailwindcss.com/docs/justify-content


# Compiled tests (row)
<Unstyled>
  <Section className="grid grid-cols-4">
  {
    variants.base.justifyContent.map((value, idx) => {
      const jc_value = value.split('-')[1]

    return <Section 
        margin={{all: '1'}} 
        height={{ value: '40'}}
        flex={{  direction: 'col',  justifyContent: jc_value }} 
        colors={{  background: { color: 'teal'} }}>
          <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
          <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
          <Section margin={{x: "2"}} padding={{x: '2'}} colors={{ text: {color: 'white'}, background: {color: 'sky'}}}>{jc_value}</Section>
      </Section>
    })
  }
  </Section>
</Unstyled>

