import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/padding'

# Demo of supported padding values
Tailwind supports values up to margin(96), initially bennie-ui will go as far as 40.

## Example
```
<Section padding={{ all: '0' }}Content</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/spacing/margin.types.ts)

## Compiled Tests
{
  variants.base.all.map((value, idx) => {
    const [, p_value] = value.split('-')
    return <Section key={idx} width={{value: 'full'}} className="border">
      <Section 
        padding={{ all: p_value }} 
        width={{ value: 'fit'}}
        height={{ value: 'fit'}}
        className="border border-sky-500">
          {value}
      </Section>
    </Section>
  })
}


