import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { Text } from '@bennie-ui/text'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/rounding'

opacity
## Example
```
<Section rounding='large'>Content</Section>
```
## Types
[See Opacity values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/sizing/opacity.types.ts)


# Compiled tests
{
  variants.base.map((value, idx) => {
    const rounding_value =  value.split('-')[1]

    return <Unstyled key={idx}>
     <Section
      rounding={rounding_value} 
      height={{value: '20'}} 
      margin={{bottom: '8'}} 
      flex={{justifyContent: 'justify-center', alignItems: 'items-center'}}
      className="border bg-gray-200 border-gray-200">
        {rounding_value}
      </Section> 
    </Unstyled>
  })
}
