import { Unstyled } from "@storybook/blocks";
import { Section } from '@bennie-ui/section'
import { Button } from '@bennie-ui/button'
import { variants } from '@bennie-ui/baseline/tailwind/attributes/actions'

## Example
```
<Button action='primary'>content</Button>
```
## Types
[See Button values](https://github.com/bolanosdev/bennie-ui/blob/main/libraries/types/sizing/opacity.types.ts)

# Compiled examples 

{
    <Unstyled>

    <Button action='primary'>no-size</Button>
  </Unstyled>
}

{
  <Unstyled>
    <Button action='primary' scale="small" >small</Button>
  </Unstyled>
}

{
  <Unstyled>
    <Button action='primary' scale="medium" >medium</Button>
  </Unstyled>
}

{
   <Unstyled>
    <Button action='primary' scale="large" >large</Button>
  </Unstyled>
}

// TODO: Fix this, scale > class > prop,
<Unstyled>
  <Button action='primary' scale="large" padding={{all: '5'}} >with padding</Button>
</Unstyled>


// TODO: Fix this. scale > class > prop, 
// see button text and action 
{
   <Unstyled>
    <Button action='primary' scale="large" padding={{all: '5'}} className="p-10">widht padding and class</Button>
  </Unstyled>
}
